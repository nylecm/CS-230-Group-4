//package security;
//
//import java.util.regex.Matcher;
//import java.util.regex.Pattern;
//
//public class UsernameValidator<T> implements Validator {
//
//    private static final String PATTERN = "^[a-zA-Z0-9]([._-](?![._-])|[a-zA-Z0-9]){3,18}[a-zA-Z0-9]$";
//
//    private static Pattern pattern  = Pattern.compile(PATTERN);
//
////    @Override
////    public boolean isValid(String username) {
////        Matcher matcher = pattern.matcher(username);
////        return matcher.matches;
////    }
//
//    @Override
//    public boolean isValid(T username) {
//        if (username.equals(String));
//        Matcher matcher = pattern.matcher(username);
//    }
//}
